# Tagentacle：AI 时代的 ROS 🐙

[![GitHub](https://img.shields.io/badge/GitHub-Tagentacle-blue)](https://github.com/Tagentacle/tagentacle)

**Tagentacle** 是一个专为大语言模型（LLM）多智能体协同、MCP（Model Context Protocol）工具挂载而设计的极轻量级、去中心化消息总线与包管理系统。

它汲取了 **ROS 2** (Robot Operating System) 的架构灵魂——“万物皆包 (Everything is a Pkg)”与“发布/订阅通信与服务发现”，但彻底抛弃了其重量级的 C++ 依赖、DDS 协议以及复杂的构建系统，转而拥抱 AI 时代所需的极致速度与简洁。

---

## 🌟 项目愿景

Tagentacle 的核心哲学是 **AI 原生中间件**：
- **协议先行 (Protocol First)**：底层通信仅依赖 **纯 JSON over TCP**。任何语言只要能收发 JSON，就能成为 Tagentacle 节点。
- **万物皆包 (Everything is a Pkg)**：大模型角色、MCP 工具箱、系统提示词都被封装为标准的 Pkg 目录，通过 `tagentacle.toml` 声明属性。
- **节点解耦 (Node Decoupling)**：节点（Node）只是运行时的实体进程。它们通过 Tagentacle Daemon（守护进程）进行完全隔离的异步通信。

---

## ⚔️ 为什么选择 Tagentacle？ (核心优势)

在单体网关和命令行工具（CLI）统治的当下，Tagentacle 为下一代 AI 提供了“工业级”的基础设施。

| 特性 | 单体网关 (如 OpenClaw) | 命令行工具 (如 Claude Code/CC) | **Tagentacle** |
| :--- | :--- | :--- | :--- |
| **架构** | Node.js 单体巨石 | 单进程 CLI | **分布式微服务 (Rust)** |
| **稳定性** | 一处崩溃，全量掉线 | 随进程结束而终止 | **进程隔离 (容错性极强)** |
| **生命周期** | 绑定聊天窗口 (TG/WA) | 任务型 (一次性) | **持续运行 (24/7 事件驱动)** |
| **交互模式** | 聊天气泡 (ChatOps) | 终端输出 | **任务指挥中心 (实时仪表盘)** |
| **覆盖范围** | 单一服务器 | 本地文件系统 | **跨设备 / 跨平台协同** |

### 1. 鲁棒性：分布式总线 vs 单体网关
*   **OpenClaw 的软肋**：在一个 Node.js 进程里跑 50 个技能，意味着任何一个技能的内存溢出都会导致整个系统重启。
*   **Tagentacle 的绝对隔离**：每一个 Node 都是独立的进程。如果你的“推特爬虫”节点由于异常崩溃，你的“运维 Agent”依然在后台稳健地监控服务器。基于 Rust 编写的 Broker 提供永不掉线的高并发消息路由。

### 2. 自主性：事件驱动生命体 vs 任务型工具
*   **超越“请求-响应”**：Claude Code 类工具只有在你输入指令时才会动作。它们在命令结束时就“死”了。
*   **活着的系统**：Tagentacle 的 Agent 是 24 小时在线的。它们可以在凌晨 3 点监控日志、发现异常、触发告警 Agent 并自主修复问题。它不是一个工具，而是一个**数字生命体**。

### 3. 专业级：任务指挥中心 vs 聊天气泡
*   **态大于流**：目前的框架大多被迫将所有信息挤进 Telegram 或 WhatsApp。
*   **专业可视化**：Tagentacle 暴露原生数据总线，允许构建类似 Foxglove 的“任务指挥中心”。你可以实时看到 Agent 间的拓扑关系、服务器 CPU 波形图、以及 Agent 正在编写的代码流——这一切都由同一套消息总线驱动。

---

## 🏗️ 系统架构

本项目由三个核心部分组成：

1.  **`tagentacle` (Rust)**：高性能消息路由器 (Daemon/Broker) 与命令行工具。 (核心仓库: `tagentacle-core`)
2.  **`tagentacle-py` (Python)**：官方 Python SDK (类比 ROS 的 `rclpy`)，为开发者提供极简的异步 API。
3.  **`tagentacle-ecosystem` (计划中)**：标准的 Pkg 集合 (如 `chatbot_ui_pkg`, `mcp_sqlite_wrapper_pkg`)。

### 🧩 ROS 2 概念映射 (最新)

Tagentacle 深度复用了 ROS 2 的工程组织哲学，并将其映射到 AI 智能体开发中：

| ROS 2 概念 | Tagentacle 映射 | AI 场景说明 |
| :--- | :--- | :--- |
| **Workspace** | **Agent 工作空间** | 包含多个 Pkg 的目录，代表一个复杂机器人智能体（如“私人助理”）。 |
| **Node** | **MCP 服务 / Agent** | 运行实体。可以是提供工具调用的 MCP Server，也可以是具备独立上下文的单体 Agent（如 Zeroclaw）。 |
| **Topic** | **配置 / 身份 / 技能 / 流** | 异步数据流。如启动时加载配置及身份，或 Web UI 发布用户输入。 |
| **Service** | **工具调用 (开发中)** | 同步 RPC 调用。通常用于高频 MCP 工具调用（如读写文件、查询数据库）。 |
| **Bringup Pkg** | **配置包** | 负责全系统的启动参数，定制化小智能体的技能、权限及运行环境变量。 |
| **Library Pkg** | **纯提示词 / 代码库** | 包含代码库或系统提示词（Skills），不启动独立节点。 |
| **Msg/Srv Pkg** | *(已简化)* | 拥抱 **JSON First**，协议层无须预定义的 IDL 消息包。 |

---

## 📜 原始通信协议规范

Tagentacle Daemon 默认监听本地 `TCP 19999` 端口。所有通信均为换行符分割的 JSON 字符串（JSON Lines）。

### 话题 (Topic)
*   **订阅**: `{"op": "subscribe", "topic": "/chat/global", "node_id": "alice_node"}`
*   **发布**: `{"op": "publish", "topic": "/chat/global", "sender": "bob_node", "payload": {"text": "Hello!"}}`
*   **消息推送 (Daemon -> Client)**: `{"op": "message", "topic": "/chat/global", "sender": "bob_node", "payload": {"text": "Hello!"}}`

---

## 🛠️ 命令行工具 (`tagentacle`)

CLI 是开发者的主要交互入口：
- `tagentacle daemon`: 启动本地 TCP 消息总线。
- `tagentacle run <pkg_name>`: 在工作区中拉起一个 Node。
- `tagentacle launch <config.yaml>`: 根据配置文件同时并行启动多个 Node。
- `tagentacle topic list/echo`: 调试总线及查看实时消息流。

---

## 🚀 快速开始

1. **启动守护进程 (Rust Core)**:
   ```bash
   cd tagentacle-core
   cargo run
   ```

2. **运行节点 (Python SDK)**:
   ```python
   from tagentacle_py import Node
   import asyncio

   async def main():
       node = Node("example_node")
       await node.connect()
       await node.publish("/hello", {"data": "world"})
       await node.spin()

   asyncio.run(main())
   ```
